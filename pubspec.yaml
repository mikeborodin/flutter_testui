name: testui3
description: A sample command-line application with basic argument parsing.
version: 0.0.1
publish_to: none

environment:
  sdk: 3.8.1

# Add regular dependencies here.
dependencies:
  args: ^2.5.0
  dart_console: ^4.1.2
  pixel_prompt:
    git: https://github.com/primequantuM4/pixel_prompt

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
